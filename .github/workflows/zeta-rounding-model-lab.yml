name: Zeta rounding model lab
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'zeta53_rounding_model_lab.cpp'
      - '.github/workflows/zeta-rounding-model-lab.yml'
jobs:
  lab:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Dependencies
        run: |
          sudo find /etc/apt/sources.list.d -maxdepth 1 -type f \( -iname '*microsoft*' -o -iname '*azure*' \) -delete || true
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends libflint-dev libmpfr-dev
      - name: Build
        run: g++ -O3 -march=native -std=c++20 -DNDEBUG zeta53_rounding_model_lab.cpp -lflint -lmpfr -lgmp -lm -o lab
      - name: Run
        shell: bash
        run: |
          set -euo pipefail
          ./lab 2>&1 | tee result.txt
      - uses: actions/upload-artifact@v4
        with:
          name: zeta-rounding-model-lab
          path: result.txt
